server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/grizlen/spring-market-config
          default-label: main
          #          username: user
          #          password: 123123
          clone-on-start: true